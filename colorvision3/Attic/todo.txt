"To Do" list for "colorvision3" project

HIGH:
o show filter transmission width on filter spectrum slider

MEDIUM:
o get rid of ColorChangeListener and replace with Observer

LOW:
o use Point for specifying locations that are based on int

IDEAS:
o put bulb color & filter color on a single spectrum slider so it's easier to see how they compare
o add a UI control for filter transmission width (in nm)