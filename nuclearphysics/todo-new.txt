Possilbe performance problem: TxGraphic.getBounds(). It calls createTransformedShape();

Add setSimulationTimeChange() and other mutators to IClock

See if we need DeveloperControlPanel (or whatever it's called) and the ModelSlider
that's in coreadditions