build_natives.sh exists so that we can build the JARs needed for access when run from JNLP for each system.
applet/basic provides signed versions of these, but it is easier to build our own to ensure we are using the
same code for JAR/JNLP methods.
